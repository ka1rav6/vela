{
    "name": "velang",
    "version": "0.1.0",
    "description": "Velang rule engine custom language lexer",
    "type": "module",
    "scripts": {
        "build": "tsc",
        "clean": "rm -rf dist",
        "test": "tsc && node --test dist/test.js"
    },
    "devDependencies": {
        "@types/node": "^22.0.0",
        "typescript": "^5.7.0"
    }
}
